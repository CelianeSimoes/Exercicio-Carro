using System;
using System.Collections.Generic;
using System.Text;

namespace Exercicio_Carro
{
    class Carro
    {

        public string Nome;
        public string Cor;
        public string Modelo;
        public double Preco;


        public Carro(string nome, string cor, string modelo, double preco)
        {
            Nome = nome;
            Cor = cor;
            Modelo = modelo;
            Preco = preco;
        }

        public double Andar()
        {

        }

        public string RadarNaFrente()
        {
            if (RadarNaFrente = sim)
            {
                return Console.WriteLine("Radar a frente - reduzindo velocidade")
                }
            else
            {
                return Console.WriteLine("Tudo Ok! Pisa fundo Bino!!!");
            }

            public string Parar()
            {

            }
            public string DescricaoCompleta(string nome, string cor, string modelo, double preco)
            {
                this.DescricaoCompleta = DescricaoCompleta;
            }

            public string AplicarDesconto()
            {
                if (corOpcao == branco || preto)
                {
                    return precoComDesconto = preco - (preco * 10);
                }
                else
                {
                    return DescricaoCompleta;
                }
            }


        }
    }
}
